name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      
      - name: Build
        run: swift build
      
      - name: Test
        run: swift test
      
      - name: SwiftLint smoke
        run: |
          brew install swiftlint
          ruby -e 'require "yaml"; YAML.load_file(".swiftlint.yml")'
          swiftlint version
